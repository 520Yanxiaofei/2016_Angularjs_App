angular.module("App.Sidebar", []).controller("App.Sidebar.Controller", [
    "$scope",
    "$state",
    function(
        $scope,
        $state
    ) {

        $scope.$state = $state
        

    }
]);