angular.module('App.Orders', ['App.Orders.All','App.Orders.Detail']).controller('App.Orders.Controller', [
    '$scope',
    '$state',
    function(
        $scope,
        $state
    ) {

       
    }
]);