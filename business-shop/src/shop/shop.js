angular.module("App.Shop", ["App.Shop.Album","App.Shop.SellerHome","App.Shop.Share"]).controller("App.Shop.Controller", [
    "$scope",
    "$state",
    function(
        $scope,
        $state
    ) {

    	
    }
]);