angular.module("App.Shop.Share", []).controller("App.Shop.Share.Controller", [
    "$scope",
    "$state",
    "Shop",
    function(
        $scope,
        $state,
        Shop
    ) {

       

    }
]);
